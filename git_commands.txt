//git version --> to check the git version 
// git init to intialize a empty git repositry
//git status --> to check the branch status 
// git add filename --> to add the file into the staging area only a single file can be added
// git add . --> to add all files to stating which are untracked 
// git commit -m "Messsage you want to pass in the commit"
// git  config --global user.name "username"
// git branch branchname --> to switch the branch in git 
// git checkout brnachname --> to checkout switch branch in git 
// git push -u origin main --> to push code in origin main 
// git push 
